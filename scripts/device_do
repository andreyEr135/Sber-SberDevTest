cd $(dirname $0)

if [ ! -f ./tests.cmake ]; then
    echo "ERROR: tests.cmake not found. (not device dir)"
    exit -1
fi

DEVICE=$(basename $(pwd))

cd ../..

./clear

ln -sf ../devices/$DEVICE/tests.cmake ./sberdevtest/Device.cmake

rm ./bin
ln -sf ./devices/$DEVICE/bin ./bin

./build
